# Phase 4 handoff: results products

- Six code-generated figures and two code-generated tables are linked to the
  same configuration and results files.
- `results/manuscript_values.csv` is the sole numerical-value registry used by
  the document builder.
- `result_traceability.csv` links claims to datasets, fields, scripts,
  configuration, and displayed products.

Next gate: build the journal-specific manuscript and supplement.
